
    const divide = (n1, n2) => {
        let result = n1 / n2;
        return result;
    }

    const multiply = (n1, n2) => {
        let result = n1 * n2;
        return result;
    }

     const sub = (n1, n2) => {
        let result = n1 - n2;
        return result;
    }

    export {divide, multiply, sub};
